[project]
name = "qubik"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "docker>=7.1.0",
]
license = {text = "MIT"}

[project.scripts]
qubik = "qubik.main:main"
